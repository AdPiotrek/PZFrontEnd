import { GroupUsersPipe } from './group-users.pipe';

describe('GroupUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
